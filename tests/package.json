{
  "name": "model-card-tests",
  "private": true,
  "description": "Playwright regression tests for the JS and React model-card renderers, run against examples/*.json",
  "scripts": {
    "test": "playwright test",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "esbuild": "^0.23.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
